# Local app
PORT=8080
HOST=127.0.0.1
SESSION_DAYS=30

# Optional JSON fallback storage (used when DATABASE_URL is not set)
DATA_DIR=./backend/data

# PostgreSQL (Supabase/Neon/Render Postgres)
DATABASE_URL=
DB_SSL=require
